apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: dex-actualbudget-client
  namespace: actualbudget
spec:
  refreshInterval: 3m
  secretStoreRef:
    kind: ClusterSecretStore
    name: oracle-vault
  target:
    name: dex-actualbudget-client
    creationPolicy: Owner
    deletionPolicy: Delete
    template:
      engineVersion: v2
      data:
        actualbudget.client-id: actualbudget
        actualbudget.client-secret: "{{ .pw }}"
  data:
    - secretKey: pw
      remoteRef:
        key: dex-actualbudget-client-secret
        version: CURRENT
        decodingStrategy: None
